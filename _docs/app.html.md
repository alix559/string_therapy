# App


<!-- WARNING: THIS FILE WAS AUTOGENERATED! DO NOT EDIT! -->

## Imports and environment

Load `.env` via `therapy_config` before importing icons that read env
vars. Icon **1** is the markdown notebook UI; icon **4** embeds Jupyter
Lab and exposes helpers used by the chat handler.

## App factory, registry, dock

`fast_app` supplies `rt` for route decorators. We register each icon’s
HTTP partials, then map slot numbers to **callables**
(`[`viz`](https://alix559.github.io/string_therapy/icon1.html#viz)`),
not dataclasses.
`[`\_dock_icon_ids`](https://alix559.github.io/string_therapy/app.html#_dock_icon_ids)`
filters `ST_ICON_IDS` / defaults so only implemented slots get
buttons—currently **1** and **4**.

------------------------------------------------------------------------

<a
href="https://github.com/alix559/string_therapy/blob/main/string_therapy/app.py#L78"
target="_blank" style="float:right; font-size:smaller">source</a>

### render_icon

``` python

def render_icon(
    icon_id:int, session:dict
):

```

*Call self as a function.*

------------------------------------------------------------------------

<a
href="https://github.com/alix559/string_therapy/blob/main/string_therapy/app.py#L55"
target="_blank" style="float:right; font-size:smaller">source</a>

### health

``` python

def health(
    
):

```

*Call self as a function.*

------------------------------------------------------------------------

<a
href="https://github.com/alix559/string_therapy/blob/main/string_therapy/app.py#L50"
target="_blank" style="float:right; font-size:smaller">source</a>

### favicon

``` python

def favicon(
    
):

```

*Call self as a function.*

## Chat and Lab shell helpers

`[`chat_icon4`](https://alix559.github.io/string_therapy/app.html#chat_icon4)`
runs Lisette and builds an optional OOB fragment for `#icon4-viz`. The
expanded area mounts **both** icon panels (Lab iframe uses
`hx-preserve`); `GET /expand/{id}` swaps `#expanded-space` to show one
panel and hide the other.
`[`\_expanded_space_icon4_oob`](https://alix559.github.io/string_therapy/app.html#_expanded_space_icon4_oob)`
restores the Lab view after notebook writes when needed. \## Chat and
Lab shell helpers

`[`chat_icon4`](https://alix559.github.io/string_therapy/app.html#chat_icon4)`
runs Lisette and builds an optional OOB fragment for `#icon4-viz`. The
expanded area mounts **both** icon panels (Lab iframe uses
`hx-preserve`); `GET /expand/{id}` swaps `#expanded-space` to show one
panel and hide the other.
`[`\_expanded_space_icon4_oob`](https://alix559.github.io/string_therapy/app.html#_expanded_space_icon4_oob)`
restores the Lab view after notebook writes when needed.

------------------------------------------------------------------------

<a
href="https://github.com/alix559/string_therapy/blob/main/string_therapy/app.py#L118"
target="_blank" style="float:right; font-size:smaller">source</a>

### chat_icon4

``` python

def chat_icon4(
    session:dict, msg:str, max_tokens:int
):

```

*Lab LLM + OOB fragment for `#icon4-viz` (same pattern as
`string therapy1`).*

## Routes

`GET /` shows the main layout with Lab visible by default.
`GET /expand/{id}` re-renders `#expanded-space` with the correct panel
visible (both stay mounted; Jupyter iframe is preserved). `POST /chat`
forwards to Lisette, appends cells via Jupyter Server when possible, and
returns status (plus OOB updates mirroring `string therapy1`).## Routes

`GET /` shows the main layout with Lab visible by default.
`GET /expand/{id}` re-renders `#expanded-space` with the correct panel
visible (both stay mounted; Jupyter iframe is preserved). `POST /chat`
forwards to Lisette, appends cells via Jupyter Server when possible, and
returns status (plus OOB updates mirroring `string therapy1`).

------------------------------------------------------------------------

<a
href="https://github.com/alix559/string_therapy/blob/main/string_therapy/app.py#L297"
target="_blank" style="float:right; font-size:smaller">source</a>

### post_chat

``` python

async def post_chat(
    msg:str, session, active_icon:str=None, vault_context:str='', notebook:str | None=None,
    jupyter_cell_type:str='markdown'
):

```

*Call self as a function.*

------------------------------------------------------------------------

<a
href="https://github.com/alix559/string_therapy/blob/main/string_therapy/app.py#L291"
target="_blank" style="float:right; font-size:smaller">source</a>

### get_expand

``` python

def get_expand(
    id:int, session
):

```

*Call self as a function.*

------------------------------------------------------------------------

<a
href="https://github.com/alix559/string_therapy/blob/main/string_therapy/app.py#L196"
target="_blank" style="float:right; font-size:smaller">source</a>

### home

``` python

def home(
    session
):

```

*Call self as a function.*

## `[`main()`](https://alix559.github.io/string_therapy/app.html#main)`

Starts **uvicorn** on `ST_BIND_HOST:ST_PORT` (defaults `0.0.0.0:5007`).
Run `jupyter lab` yourself; icon 4 shows a link (`ST_JUPYTER_LAB_URL` /
placeholder). Use `http://127.0.0.1:ST_PORT/` or your host IP when not
browsing on the same machine.##
`[`main()`](https://alix559.github.io/string_therapy/app.html#main)`

Starts **uvicorn** on `ST_BIND_HOST:ST_PORT` (defaults `0.0.0.0:5007`).
Run `jupyter lab` yourself; icon 4 shows a link (`ST_JUPYTER_LAB_URL` /
placeholder). Use `http://127.0.0.1:ST_PORT/` or your host IP when not
browsing on the same machine.

------------------------------------------------------------------------

<a
href="https://github.com/alix559/string_therapy/blob/main/string_therapy/app.py#L359"
target="_blank" style="float:right; font-size:smaller">source</a>

### main

``` python

def main(
    
)->None:

```

*Start the web server. Run: `python -m string_therapy.app`.*
