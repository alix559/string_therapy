# icon1


<!-- WARNING: THIS FILE WAS AUTOGENERATED! DO NOT EDIT! -->

## Visualization layer

This Python code implements a dynamic, Jupyter-like notebook interface
using FastHTML and HTMX. It parses Markdown into distinct text and code
“cells,” styling them with MonsterUI. Users can toggle between a
live-updating editor and a rendered view, enabling seamless, interactive
document creation without full page reloads.

------------------------------------------------------------------------

<a
href="https://github.com/alix559/string_therapy/blob/main/string_therapy/icon1.py#L75"
target="_blank" style="float:right; font-size:smaller">source</a>

### icon_panel

``` python

def icon_panel(
    md:str, editor_open:bool
):

```

*Root fragment; id=icon-panel is the HTMX swap target.*

------------------------------------------------------------------------

<a
href="https://github.com/alix559/string_therapy/blob/main/string_therapy/icon1.py#L58"
target="_blank" style="float:right; font-size:smaller">source</a>

### notebook_editor_panel

``` python

def notebook_editor_panel(
    md:str
):

```

*Call self as a function.*

------------------------------------------------------------------------

<a
href="https://github.com/alix559/string_therapy/blob/main/string_therapy/icon1.py#L42"
target="_blank" style="float:right; font-size:smaller">source</a>

### parse_to_notebook

``` python

def parse_to_notebook(
    md_text
):

```

*Call self as a function.*

------------------------------------------------------------------------

<a
href="https://github.com/alix559/string_therapy/blob/main/string_therapy/icon1.py#L29"
target="_blank" style="float:right; font-size:smaller">source</a>

### NotebookCell

``` python

def NotebookCell(
    content, cell_type:str='markdown'
):

```

*Call self as a function.*

------------------------------------------------------------------------

<a
href="https://github.com/alix559/string_therapy/blob/main/string_therapy/icon1.py#L124"
target="_blank" style="float:right; font-size:smaller">source</a>

### default_icon1_viz_layer

``` python

def default_icon1_viz_layer(
    
)->Icon1VizLayer:

```

*Call self as a function.*

------------------------------------------------------------------------

<a
href="https://github.com/alix559/string_therapy/blob/main/string_therapy/icon1.py#L116"
target="_blank" style="float:right; font-size:smaller">source</a>

### Icon1VizLayer

``` python

def Icon1VizLayer(
    notebook_initial_md:str, icon_panel:Callable, parse_to_notebook:Callable
)->None:

```

*Injectable notebook UI (same role as `visualizations.icon1.viz` in
string therapy1).*

## Controller Layer

This code acts as the controller for the notebook visualization layer.
It manages the “source of truth” for the markdown content and handles
the state(weather the user is currently editing or viewing) HEre is the
breakdown of how it works

### 1. State management (the session)

the code uses a `session` dictionary to keep the notebook’s data alive.
This means the content is per-user and temporary. If they clear their
browser cookies or the session expires, the notebook resets. \>
`SESSION_MD_KEY` : Stores the actual markdown text the user types

> `SESSION_EDITOR_KEY`: A boolean (True/False) that remembers if the
> user was in Edit mode or view mode

### 2. Helper Logic: \_md

``` python
def _md(session: dict) -> str:
    return session.get(SESSION_MD_KEY) or NOTEBOOK_INITIAL_MD
```

This is a safety check. It tries to grab the user’s saved markdown. If
the session is empty (like when they first land on the page), it falls
back to the `NOTEBOOK_INITIAL_MD` default text### 2. Helper Logic: \_md

``` python
def _md(session: dict) -> str:
    return session.get(SESSION_MD_KEY) or NOTEBOOK_INITIAL_MD
```

This is a safety check. It tries to grab the user’s saved markdown. If
the session is empty (like when they first land on the page), it falls
back to the `NOTEBOOK_INITIAL_MD` default text

### 3. The Routes(the “wiring”)

`GET /icon/panel` This handles the toggle button logic. \* When you
click “Edit,” it hits this route with edit=1. The session updates
`SESSION_EDITOR_KEY` to `True` \* When you click “Done”, it hits this
route with `edit=0` \* it then returns the
[`icon_panel`](https://alix559.github.io/string_therapy/icon1.html#icon_panel)
HTML, which decides wheather to show the `Textarea` (editor) or the
[`parse_to_notebook`](https://alix559.github.io/string_therapy/icon1.html#parse_to_notebook)
(rendered cells)

`POST icon/render` This is the Live update engine \* Remember the
`hx_trigger="keyup changed delay:300ms` from the vizualization layer.
Every time you pause typing for 300ms, it sends the text here \* it
saves that text into the session (`session[SESSION_MD_KEY] = content`).
\* It then returns the Notebook view (the rendered cells ) so the UI can
update immediately.

------------------------------------------------------------------------

<a
href="https://github.com/alix559/string_therapy/blob/main/string_therapy/icon1.py#L147"
target="_blank" style="float:right; font-size:smaller">source</a>

### register_routes

``` python

def register_routes(
    app, viz_layer:__main__.Icon1VizLayer | None=None
):

```

*Call self as a function.*

------------------------------------------------------------------------

<a
href="https://github.com/alix559/string_therapy/blob/main/string_therapy/icon1.py#L141"
target="_blank" style="float:right; font-size:smaller">source</a>

### viz

``` python

def viz(
    sid:str, session:dict, viz_layer:__main__.Icon1VizLayer | None=None
):

```

*Call self as a function.*
